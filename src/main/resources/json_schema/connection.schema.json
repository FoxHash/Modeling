{
  "$schema" : "http://json-schema.org/draft-07/schema#",
  "title" : "Connection",
  "description" : "A connection between an entity and a relationship of an erd.",
  "type" : "object",
  "properties" : {
    "id" : {
      "description" : "Name of the connection",
      "type" : "string"
    },
    "relationship" : {
      "description" : "Name of the relationship which will be connected",
      "type" : "string"
    },
    "entity" : {
      "description" : "Name of the entity which will be connected",
      "type" : "string"
    },
    "cardinality" : {
      "description" : "Cardinality of the connection between the relationship and the entity",
      "type" : "string"
    }
  },
  "required" : [
    "id",
    "relationship",
    "entity",
    "cardinality"
  ]
}